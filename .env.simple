# PlaneHelper 示例环境变量
# 该文件用于说明可选配置，允许提交到 Git。

# 后端监听地址，设置为 0.0.0.0 后可被局域网设备访问
HOST=0.0.0.0

# 后端端口
PORT=3001

# 前端默认通过相对路径访问 /api 和 /uploads
# 如需显式指定后端地址，可在 frontend/.env.local 中配置：
# VITE_API_BASE=http://<your-lan-ip>:3001
